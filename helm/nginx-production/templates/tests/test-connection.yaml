apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-production.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-production.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-production.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
