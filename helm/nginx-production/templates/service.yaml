apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-production.fullname" . }}
  labels:
    {{- include "nginx-production.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-production.selectorLabels" . | nindent 4 }}
